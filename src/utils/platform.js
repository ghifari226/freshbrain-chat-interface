// Mocked for now: no native platform is wired up yet (Capacitor is scaffolded
// but `cap add android` hasn't run). Flip to `true` locally to preview native
// layouts in a browser, then flip back before committing.
export const isNativeApp = false
